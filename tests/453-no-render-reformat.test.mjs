import test from 'node:test';import assert from 'node:assert/strict';import crypto from 'node:crypto';import fs from 'node:fs';test('453 exact proven render blob',()=>{const b=fs.readFileSync(new URL('../src/render-modes.js',import.meta.url));assert.equal(crypto.createHash('sha1').update(Buffer.concat([Buffer.from(`blob ${b.length}\0`),b])).digest('hex'),'977dda1ae40dd643e6fae95df075899db0191f64');});
