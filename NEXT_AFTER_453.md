# After .453

Only after hands-on confirmation that .453 loads and sentinels pass, create the next Facegroups viewport integration as a minimal additive patch. Do not replace or reformat existing render runtime functions.
