import * as THREE from 'three';
const button=document.querySelector('#bevelBtn'),canvas=document.querySelector('#viewport'),width=document.querySelector('#bevelWidth'),out=document.querySelector('#bevelWidthOut'),ray=new THREE.Raycaster(),pointer=new THREE.Vector2();let armed=false,drag=null;
function state(){return globalThis.__boxlabBridgeState}function bridge(){return globalThis.__boxlabSelectionBridge}function hit(e){const s=state(),r=canvas.getBoundingClientRect();if(!s?.camera)return null;pointer.set((e.clientX-r.left)/r.width*2-1,-((e.clientY-r.top)/r.height)*2+1);ray.setFromCamera(pointer,s.camera);const h=ray.intersectObjects([...(s.edgeObjects?.values()||[])],false)[0];return Number.isInteger(h?.object?.userData?.index)?h.object.userData.index:null}
button?.addEventListener('click',e=>{if((state()?.selectedEdges||[]).length)return;e.preventDefault();e.stopImmediatePropagation();armed=!armed;button.classList.toggle('active',armed);document.querySelector('#selectionStatus').textContent=armed?'Bevel Edge • tap an edge, then drag width':'Edge mode • nothing selected'},true);
canvas?.addEventListener('pointerdown',e=>{if(!armed||!e.isPrimary)return;const i=hit(e);if(!Number.isInteger(i))return;e.preventDefault();e.stopImmediatePropagation();bridge()?.add?.('edge',i);drag={id:e.pointerId,x:e.clientX};canvas.setPointerCapture?.(e.pointerId)},true);
canvas?.addEventListener('pointermove',e=>{if(!drag||drag.id!==e.pointerId)return;const value=Math.max(2,Math.min(45,20+(e.clientX-drag.x)*.25));width.value=String(Math.round(value));out.textContent=`${Math.round(value)}%`},true);
canvas?.addEventListener('pointerup',e=>{if(!drag||drag.id!==e.pointerId)return;drag=null;armed=false;button.classList.remove('active');button.click()},true);
